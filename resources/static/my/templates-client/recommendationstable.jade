each activity in activities
	.row
			.span1
				label #{activity.content}
			.span2
			.offset
				label #{new Date(activity.date_created).toLocaleDateString()}
			.span4
				label #{activity.user_name}
			.span4
				label Recommendation
