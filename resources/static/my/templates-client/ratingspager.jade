if(action.pages>1)
	- for (var page=1; page<=action.pages; page++)
		if (act_page == page)
			div.page.act-page
				a( href="#/ratings",data-filter="#{filter}",data-id="#{page}",name="ratingpager", class="act-page") #{page}
		else
			div.page
				a( href="#/ratings",data-filter="#{filter}",data-id="#{page}",name="ratingpager") #{page}