.row
		.span1
			label #{user.first_name}
		.span2
			label #{user.last_name}
		.span2
			a( href="#" data-id="#{user.id}" name="editprofile") Edit Profile
		.span2
			a( href="#" data-id="#{user.id}" name="deleteprofile") Delete Profile

.row
	.span1
			a( href="#/profile" name="back") Go back