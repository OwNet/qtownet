.pager
	if(pages>1)
		- for (var page=1; page<=pages; page++)
			if (current == page)
				div.page.act-page
					a.act-page(href="#/files", data-filter="#{filter}",data-id="#{page}") #{page}
			else
				div.page
					a( href="#/files", data-filter="#{filter}", data-id="#{page}") #{page}
